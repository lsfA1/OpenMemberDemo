# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'OpenMemberDemo' do
  
  pod 'Masonry', '~> 1.1.0'

  # Uncomment the next line if you're using Swift or would like to use dynamic frameworks
  # use_frameworks!

  # Pods for OpenMemberDemo

  target 'OpenMemberDemoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'OpenMemberDemoUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
